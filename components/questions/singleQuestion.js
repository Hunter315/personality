import React from 'react';

const SingleQuestion = (props) => {

    // render question, record response and send response to database


    return(
        <>
        <h4>{props.title}</h4>
        </>
    )



}

export default SingleQuestion;